//Execution starts from Main
fun main() {

    val fullName: String = "Hemanshu Varma"

    println(fullName.decapitalize()) //prints string with first letter converted to lowercase

    println(fullName.toLowerCase()) // prints whole string in lowercase

    val c: Char = 'H' // 16-bit unicode character.
    //NOTE:
    // 1. Allows only a single character, represented with single quotes
    // 2. Allows escape characters( \n, \t, \\ and so on...)

}